version: "3.9"
services:
  app:
    build:
      dockerfile: Dockerfile
      context: .
      target: base
    volumes:
      - .:/app
    ports:
      - "4000:4000"
    links:
      - db
  
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: snippetbox
      MYSQL_USER: web
      MYSQL_PASSWORD: pass
    volumes:
      - mysql:/var/lib/mysql
volumes:
  mysql:
    